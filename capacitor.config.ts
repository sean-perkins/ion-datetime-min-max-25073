import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-datetime-min-max-25073',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
